export default () => {};

// Interface declaration for AboutItem
interface WorkshopRouteParameters {
  guid: string;
  selected: {
    serviceType: string;
    carBrand: string;
    city: string;
  };
}

