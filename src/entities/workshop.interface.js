export default () => {};

// Interface declaration for Workshop
interface WorkshopInterface {
  id: string;
  name: string;
  email: string;
  phone: string;
  city: string;
  postalCode: string;
  address: string;
}
